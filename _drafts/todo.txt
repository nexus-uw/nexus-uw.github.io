make post about:
- https://github.com/nexus-uw/Hidden-Docker-Limesurvey
- how much faster (redisClient.multi(...sets).exec()).then is than Promise.all(sets).then
- how to change aws sdk api call timeout (required for long running lambdas)
- switching from nginx to caddy (inside docker)
- how to view caddy log files using goaccess
- spell checker as additional check const a = Object.assign({}, base, { flightNumber: 1 }); const a = Object.assign({}, base, { flighNumber: 1 }); wont be caught by typescript
- https://github.com/nexus-uw/delay-server
